package com.flir.thermalsdk;

/**
 * Stub enum for FLIR SDK ErrorCode - compile-time only.
 */
public enum ErrorCode {
    OK,
    GENERIC_ERROR,
    CAMERA_ERROR,
    CONNECTION_ERROR,
    TIMEOUT,
    CANCELLED
}
